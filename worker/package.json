{
  "name": "astrbot-daily-report-worker",
  "private": true,
  "version": "0.1.0",
  "cloudflare": {
    "bindings": {
      "REPORTS": {
        "description": "KV Namespace used to store generated web report JSON."
      },
      "UPLOAD_TOKEN": {
        "description": "Shared secret used by the plugin and Worker."
      },
      "TEMPLATE_SOURCE_REPO": {
        "description": "Template source repo. Default: SXP-Simon/astrbot_plugin_qq_group_daily_analysis"
      },
      "TEMPLATE_SOURCE_REF": {
        "description": "Template source ref. Default: main"
      },
      "TEMPLATE_SOURCE_PATH": {
        "description": "Template source path. Default: src/infrastructure/reporting/templates"
      }
    }
  },
  "scripts": {
    "sync-templates": "python3 scripts/sync_templates.py",
    "deploy": "python3 scripts/sync_templates.py && python3 -m pip install --disable-pip-version-check uv >/dev/null && uv run pywrangler deploy",
    "dev": "python3 scripts/sync_templates.py && python3 -m pip install --disable-pip-version-check uv >/dev/null && uv run pywrangler dev"
  }
}
